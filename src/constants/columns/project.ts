import type { Column } from '@/types/common';

export const PROJECT_COLUMNS: Column[] = [
  { field: 'index' },
  { field: 'title' },
  { field: 'type' },
  { field: 'createdAt', align: 'center' },
];
